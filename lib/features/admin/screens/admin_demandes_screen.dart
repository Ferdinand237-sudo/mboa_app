import 'package:flutter/material.dart';
class AdminDemandesScreen extends StatelessWidget {
  const AdminDemandesScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Demandes')));
  }
}
